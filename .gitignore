# OS
.DS_Store
Thumbs.db
*.swp
*~

# Editors
.vscode/
.idea/
*.code-workspace

# Python (used by tools/build-pdf.sh)
__pycache__/
*.pyc
.venv/
venv/

# Node (md-to-pdf fallback)
node_modules/

# MetaEditor build artifacts (we DO commit .mq4/.mq5/.mqh sources, but NOT compiled binaries)
*.ex4
*.ex5

# TradingView
# (nothing to ignore — Pine source is the only artifact)

# Local PDF rebuilds — committed copy lives in dist/, intermediate scratch is ignored
dist/*.html
dist/*.tmp

# Personal trade journals (if users add their own under examples/journals/)
examples/journals/personal/
